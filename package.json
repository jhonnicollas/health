{
  "name": "hl-health-companion",
  "private": true,
  "version": "1.0.0",
  "description": "HL Health Companion - PWA & Hono Worker",
  "workspaces": [
    "web",
    "worker"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:worker": "npm run dev --workspace=worker"
  }
}
